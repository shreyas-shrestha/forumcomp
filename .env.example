# Copy to .env and fill in (never commit .env).
# Use the read+trade key for python bot.py --live --ws.

FORUM_API_KEY=
FORUM_API_SECRET=

# Optional overrides:
# FORUM_API_BASE=https://comp-api.forum.market/v1
# FORUM_WS_URL=wss://comp-api.forum.market/ws/v1
